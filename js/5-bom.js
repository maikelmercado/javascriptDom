'use strict'

// BOM Browser Objet Model : ver que tamaño tiene la ventana del navegador, como dirigir una url a otra forman parte del navegador.
// modelo de objto del navegador

console.log(window.innerWidth); //metodo window objeto acceder anchura de la pantalla
console.log(window.innerHeight); //altura
console.log(screen.height); //ACCEDE A LAS PROPIEDADES DEL NAVEGADOR
console.log(window.location.href); // esta todo lo que esta n la web href la url 

/*function redirect (url){
    window.location.href = url;
}
 redirect();*/

